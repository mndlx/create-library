{
  "name": "virtuallab-create-library",
  "version": "1.1.0",
  "description": "Scaffold and configure project templates from a manifest-driven engine.",
  "author": "Mariano De Luca",
  "license": "ISC",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "dev": "node ./bin/generate.js",
    "bo": "node ./bin/bo.js",
    "prepublishOnly": "tsc",
    "deploy": "yarn run build && yarn publish",
    "bo:web": "node ./bin/bo-web.js",
    "bo:web:ui": "yarn --cwd bo-web dev",
    "bo:web:build": "yarn --cwd bo-web install && yarn --cwd bo-web build"
  },
  "bin": {
    "virtuallab-create-library": "bin/generate.js",
    "virtuallab-create-library-bo": "bin/bo.js",
    "virtuallab-create-library-bo-web": "bin/bo-web.js"
  },
  "files": [
    "bin",
    "engine",
    "templates",
    "web"
  ],
  "dependencies": {
    "@virtual-registry/mandolin": "^1.1.9"
  },
  "devDependencies": {
    "@types/node": "^20.2.1",
    "typescript": "^5.0.4"
  }
}
